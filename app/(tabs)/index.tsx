import React from 'react';
import { SafeAreaView } from 'react-native';
import Perpus from '../no1';
import AddContactForm from '../no3';
import UserList from '../no2';

const App = () => {
  return (
    <SafeAreaView style={{ flex: 1 }}>
      <Perpus/>
    </SafeAreaView>
  );
};

export default App;
